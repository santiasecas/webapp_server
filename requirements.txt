# ═══════════════════════════════════════════════════════
#  Corporate Platform — Python Dependencies
#  Install with: pip install -r requirements.txt
# ═══════════════════════════════════════════════════════

# Web framework
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.12       # Required for Form() in FastAPI

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0                # Async PostgreSQL driver
psycopg2-binary==2.9.10        # Sync driver (for migrations/scripts)

# Auth
bcrypt==4.2.1
itsdangerous==2.2.0            # Session cookie signing (HMAC-SHA256)

# Config & validation
pydantic==2.10.3
pydantic-settings==2.6.1
pydantic[email]                # EmailStr support
python-dotenv==1.0.1

# Templates
jinja2==3.1.4

# HTTP client (useful for inter-service calls)
httpx==0.28.0

# Optional: APR1-MD5 htpasswd support on non-Linux systems
# passlib[bcrypt]==1.7.4

# ── Dev / testing (not needed in production) ──────────────────
# pytest==8.3.4
# pytest-asyncio==0.24.0
# httpx==0.28.0               # Already included above
# aiosqlite==0.20.0           # SQLite for tests (no Postgres needed)
